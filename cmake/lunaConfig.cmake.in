@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@LUNA_WINDOW_SDL2@)
  find_dependency(SDL2 REQUIRED)
endif()

if(@LUNA_WINDOW_GLFW@)
  find_dependency(glfw3 REQUIRED)
endif()

if(@LUNA_GLM@)
  find_dependency(glm REQUIRED)
endif()

find_dependency(fmt REQUIRED)

set(LUNA_MODULE_DIR "${CMAKE_CURRENT_LIST_DIR}")
list(APPEND CMAKE_MODULE_PATH "${LUNA_MODULE_DIR}")

include("${CMAKE_CURRENT_LIST_DIR}/LunaTargets.cmake")
